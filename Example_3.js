/*
* In this example we will quickly see how can define array and loops in JS. No surprise. the
* following codes are identical to other programming languages.
* */
let numbers = [12, 78, -45, 197, 22]
for (let i = 0; i < numbers.length; i++){
    console.log(numbers[i])
}
